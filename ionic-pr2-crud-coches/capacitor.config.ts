import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-pr2-crud-coches',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
